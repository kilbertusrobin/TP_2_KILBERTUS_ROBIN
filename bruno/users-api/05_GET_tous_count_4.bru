meta {
  name: 05 - GET tous les utilisateurs (count 4)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.count: eq 4
}
